<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.meeting_list.UpdateMeetingActivity"
            android:label="@string/activity_update_meeting"
            android:exported="false" />
        <activity
            android:name=".ui.meeting_list.MeetingDetailsActivity"
            android:label="@string/activity_detail_meeting"
            android:exported="false" />
        <activity
            android:name=".ui.meeting_list.AddMeetingActivity"
            android:label="@string/activity_new_meeting" />
        <activity
            android:name=".ui.meeting_list.ListMeetingActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>